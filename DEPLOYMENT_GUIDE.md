
# 🚀 Deployment Guide: Deploying NBK Golf to Vercel

คุณพร้อมที่จะนำเว็บไซต์ขึ้นออนไลน์แล้ว! นี่คือขั้นตอนแบบ Step-by-Step

## 1. เตรียมความพร้อม (Prerequisites)

-   มีบัญชี [Vercel](https://vercel.com/) (Login ด้วย GitHub แนะนำ)
-   Push code ล่าสุดขึ้น GitHub repository แล้ว

## 2. สิ่งที่ต้องเตรียม (Environment Variables)

ก่อน deploy ให้ copy ค่าเหล่านี้จากไฟล์ `.env.local` มาเก็บไว้ใน Notepad ก่อน เพราะเราต้องเอาไปใส่ใน Vercel:

| Variable Name | Description |
| :--- | :--- |
| `NEXTAUTH_URL` | URL ของเว็บจริง (เช่น `https://nbk-golf.vercel.app`) **สำคัญ:** ถ้ายังไม่รู้ URL ให้ใส่ `http://localhost:3000` ไปก่อน แล้วค่อยมาแก้ทีหลัง |
| `NEXTAUTH_SECRET` | คีย์ลับสำหรับเข้ารหัส (ใช้ค่าเดิมจากไฟล์ .env.local ได้) |
| `AUTH_LINE_ID` | LINE Login Channel ID |
| `AUTH_LINE_SECRET` | LINE Login Channel Secret |
| `NEXT_PUBLIC_ADMIN_LINE_IDS` | LINE User IDs ของ Admin (คั่นด้วยจุลภาค) |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL` | อีเมล Service Account ของ Google Sheets |
| `GOOGLE_PRIVATE_KEY` | **(จุดปราบเซียน)** Private Key ของ Google |
| `GOOGLE_SHEET_ID` | ID ของ Google Sheet ที่ใช้เก็บข้อมูล |
| `NEWS_API_KEY` | API Key สำหรับระบบข่าว |

---

## 3. ขั้นตอนการ Deploy บน Vercel

1.  ไปที่หน้า Dashboard ของ Vercel: [https://vercel.com/dashboard](https://vercel.com/dashboard)
2.  คลิกปุ่ม **"Add New..."** -> **"Project"**
3.  เลือก Repository `nbk-golf` ของคุณ แล้วกด **"Import"**
4.  ในหน้า **Configure Project**:
    -   **Framework Preset:** เลือก `Next.js` (น่าจะเลือกให้อัตโนมัติ)
    -   **Root Directory:** `./` (ไม่ต้องแก้)
    -   **Build Command:** `next build` (ไม่ต้องแก้)
5.  🔻 **Environment Variables (สำคัญมาก)**:
    -   คลิกเพื่อเปิดส่วนนี้
    -   ก๊อปปี้ค่าจากข้อ 2 มาใส่ทีละตัว (Key กับ Value)
    -   ⚠️ **เทคนิคใส่ GOOGLE_PRIVATE_KEY:**
        -   ใน Vercel, ค่า Value **ห้ามมีเครื่องหมายฟันหนู `"` ครอบ**
        -   คัดลอกตั้งแต่ `-----BEGIN PRIVATE KEY-----` จนถึง `-----END PRIVATE KEY-----` (รวม \n ได้เลย Vercel ฉลาดพอ)
        -   **ห้าม** เว้นบรรทัดเอง ให้ copy มาทั้งก้อนยาวๆ เลย
6.  กด **"Deploy"** 🚀
7.  รอสักครู่... (ประมาณ 1-2 นาที) จนกว่าจะขึ้นหน้าจอพลุ 🎉

---

## 4. หลัง Deploy เสร็จ (Post-Deployment)

### 4.1 อัปเดต URL ใน Vercel (ถ้าจำเป็น)
-   ถ้าตอนแรกดู URL ไม่ออก แล้วใส่ `localhost` ไป
-   ให้ไปที่ **Settings -> Environment Variables** ใน Vercel
-   แก้ไข `NEXTAUTH_URL` ให้เป็น Domain จริงที่ได้มา (เช่น `https://nbk-golf-xyz.vercel.app`) - **ไม่มี slash / ต่อท้าย**
-   ถ้าแก้ค่า Environment Variables ต้องไปที่ tab **Deployments** แล้วกด **Redeploy** (3 จุดมุมขวา -> Redeploy) เพื่อให้ค่าใหม่ทำงาน

### 4.2 อัปเดต LINE Developers Console
เพื่อให้ Login ได้จริง ต้องไปบอก LINE ว่าเรามี URL ใหม่แล้ว:

1.  เข้าไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2.  เลือก Provider และ Channel ของคุณ
3.  ไปที่แท็บ **LINE Login**
4.  หาหัวข้อ **Callback URL**
5.  กด **Edit** แล้วเพิ่ม URL ใหม่เข้าไป (ใส่เพิ่มได้ ไม่ต้องลบอันเก่า):
    -   รูปแบบ: `https://<ชื่อโปรเจคของคุณ>.vercel.app/api/auth/callback/line`
    -   ตัวอย่าง: `https://nbk-golf.vercel.app/api/auth/callback/line`
6.  กด **Update**

---

## 5. การทดสอบ (Verification)

1.  เปิดหน้าเว็บจริง
2.  ลอง Login ด้วย LINE
    -   ✅ ถ้าผ่าน: เยี่ยมยอด!
    -   ❌ ถ้าเจอ Error 500 หรือ Callback Error: แปลว่าใส่ Callback URL ใน LINE ผิด หรือ `NEXTAUTH_URL` ใน Vercel ไม่ตรง
3.  ลองดูข้อมูล Tournament/Score
    -   ✅ ถ้าข้อมูลขึ้น: เยี่ยม! Google Sheets เชื่อมต่อสำเร็จ
    -   ❌ ถ้าไม่ขึ้น: เช็ค `GOOGLE_PRIVATE_KEY` หรือ `GOOGLE_SHEET_ID` ใน Vercel อีกรอบ

---

## 6. 🔧 Troubleshooting: LINE Login ไม่ทำงาน

### สาเหตุที่พบบ่อยที่สุด

> [!WARNING]
> **ปัญหา: กด Login แล้วไม่มีอะไรเกิดขึ้น หรือ Error 500**

#### ✅ Quick Fix Checklist

ทำตามลำดับนี้:

1. **ตรวจสอบ Environment Variables บน Vercel**
   - ไปที่ `Settings → Environment Variables`
   - เช็คว่ามีครบ 4 ตัวนี้:
     - `NEXTAUTH_URL` = `https://nbkgolf.vercel.app` (ไม่มี `/` ท้าย)
     - `NEXTAUTH_SECRET` 
     - `AUTH_LINE_ID`
     - `AUTH_LINE_SECRET`

2. **หลังแก้ Environment Variables ต้อง Redeploy**
   - ไปที่ Tab **Deployments**
   - คลิก 3 จุด (⋮) ที่ deployment ล่าสุด
   - เลือก **Redeploy**

3. **ตรวจสอบ LINE Callback URL**
   - เข้า [LINE Developers Console](https://developers.line.biz/console/)
   - Provider → Channel → **LINE Login** tab
   - **Callback URL** ต้องมี:
     ```
     https://nbkgolf.vercel.app/api/auth/callback/line
     ```

4. **ดู Vercel Runtime Logs**
   - ไปที่ **Deployments** → Latest → **Functions** tab
   - ลอง Login อีกครั้ง
   - ดู error message ที่ `/api/auth/callback/line`

### Error Messages ที่พบบ่อย

| Error | สาเหตุ | วิธีแก้ |
|-------|--------|---------|
| **Invalid client secret** | `AUTH_LINE_SECRET` ผิด | ตรวจสอบ value บน Vercel + Redeploy |
| **Redirect URI mismatch** | Callback URL ไม่ตรงกับ LINE Console | แก้ใน LINE Developers Console |
| **NEXTAUTH_URL is undefined** | ไม่มี env var นี้ | เพิ่ม `NEXTAUTH_URL` บน Vercel |
| **500 Internal Server Error** | Environment variables ไม่ครบ | เช็คทุกตัว + Redeploy |

### ทดสอบว่าแก้ไขสำเร็จ

```bash
# 1. เปิดเว็บ
https://nbkgolf.vercel.app/

# 2. กดปุ่ม "เข้าสู่ระบบ / ลงทะเบียนด้วย LINE"

# 3. ควรเกิด flow นี้:
#    - เปิดหน้า LINE Login
#    - ใส่ข้อมูล LINE
#    - กลับมาหน้า Dashboard (สำเร็จ! ✅)
```

---

ขอให้สนุกกับเว็บใหม่ครับ! 🎉
